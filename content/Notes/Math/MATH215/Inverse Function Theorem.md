#math #math215 #ODEs

For continuously differentiable functions on an open set:

$$
\frac{dy}{dx} = f(y) \iff \frac{dx}{dy} = \frac{1}{f(y)}
$$

We can use this to obtain:

$$
x(y) = \int \frac{1}{f(y)}dy + C
$$
And then solve implicitly for $y(x)$.